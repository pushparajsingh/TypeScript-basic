"use strict";
let random = ["abc", true, 35];
let rand = ["abc", true, 45]; //it is correct thing
let ran = ["abc", true, 45]; //it is also correct thing
let arr = ["abc", "ram", "nath"]; //it is correct thing and always contain string value
let role = ['admin', 'manager', 1, 3];
role.push(true);
role[1] = "user";
