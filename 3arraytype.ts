let random: [string, boolean, number] = ["abc", true, 35];
let rand: any[] = ["abc", true, 45]; //it is correct thing
let ran = ["abc", true, 45]; //it is also correct thing
let arr: string[] = ["abc", "ram", "nath"]; //it is correct thing and always contain string value
let role :[string,string,number,number,boolean?] = ['admin','manager',1, 3];
role.push(true);
role[1] = "user";

